Overall Objectives: Add compressor slope controls, UI meters, widener, LUFS loudness control, update UI, and bit-perfect monitor

A. Objective 1
Integrate advanced playback-focused DSP enhancements into the macOS equalizer application (DHDook/equaliser-adv). This specification adds selectable Linkwitz-Riley crossover slope controls, a fixed internal soft-knee math curve for the Multiband Compressor, and an adjustable soft-knee parameter slider for the wideband Compressor.



1. UI Layout & Component Sequencing

Multiband Compressor Section Updates
Action: Add two horizontal Segmented Pickers (or segmented controls) to the existing layout block in the Dynamics menu.
Segment Labels: Each control must present two options: Gentle (maps to 24 dB/oct) and Steep (maps to 48 dB/oct).
Layout Placement:
Low/Mid Slope Picker: Position horizontally to the right of the Low/Mid Crossover text labeland to the left of the associated Low/Mid frequency slider.
Mid/High Slope Picker: Position horizontally to the right of the Mid/High Crossover text labeland to the left of the associated Mid/High frequency slider.
UI Restriction: Do not add any soft-knee sliders to this section. The multiband soft-knee parameter remains hidden from the UI to save space.

Wideband Compressor Section Updates
Action: Add a new parameter slider to the UI.
Slider Label: "Knee Width"
Placement: Position this slider directly underneath the existing Ratio Slider.
Range: 0.0 dB (Pure Hard Knee) to 20.0 dB (Maximum Soft-Knee transition zone).
Safe Default: 6.0 dB

Menu Auto-Sizing Behavior
Adjust the window height and main stack layout properties of the Dynamics menu. The window frame must auto-scale cleanly to fit these horizontal adjustments and the new wideband Knee slider. Strict Rule: Ensure everything fits beautifully on-screen so that no vertical scrolling is introduced.



2. Initialization & State Serialization
Configure these updated parameters within your application's preset serialization architecture:

swift
enum CrossoverSlope: Int {
    case gentle = 0 // 24 dB/oct (Linkwitz-Riley 4th-Order)
    case steep = 1  // 48 dB/oct (Linkwitz-Riley 8th-Order)
}

// Multiband default states
var mbSlopeLowMid: CrossoverSlope = .gentle
var mbSlopeMidHigh: CrossoverSlope = .gentle

// Wideband default states
var compressorKneeWidthDB: Float = 6.0
Use code with caution.




3. Core DSP Mathematical Framework

Part A: Multiband Crossover Slope Optimization (via Apple Accelerate vDSP)
To prevent audio dropouts over CoreAudio, do not write raw, sample-by-sample processing loops. You must leverage Apple’s Accelerate framework (vDSP_biquad or native biquad functions) to execute these operations efficiently:
Gentle Selection: Route the audio channel vectors through two (2) cascaded 2nd-order Butterworth stages in series (Linkwitz-Riley 4th-Order).
Steep Selection: Route the audio channel vectors through four (4) cascaded 2nd-order Butterworth stages in series (Linkwitz-Riley 8th-Order).
Note: Ensure biquad coefficient calculations are processed during UI event changes, rather than inside the main audio block processing stream.

Part B: Soft-Knee Gain Curve Mapping Algorithm
Apply this three-part conditional polynomial function to handle the threshold mappings for both modules.
Let \(X_{\text{dB}}\) be the input peak decibel envelope, \(T\) be the threshold, \(R\) be the ratio, and \(W\) be the knee width. Calculate the target compressed output level (\(G_{\text{target\_dB}}\)) using this logic:
Condition 1: Below the Knee Boundary
If \(X_{\text{dB}} < (T - \frac{W}{2})\):
\(G_{\text{target\_dB}}=X_{\text{dB}}\)
Condition 2: Inside the Soft-Knee Zone
If \(\left|X_{\text{dB}}-T\right|\le \frac{W}{2}\):
\(G_{\text{target\_dB}}=X_{\text{dB}}+\frac{(\frac{1}{R}-1)\cdot (X_{\text{dB}}-T+\frac{W}{2})^{2}}{2\cdot W}\)
Condition 3: Above the Knee Boundary
If \(X_{\text{dB}} > (T + \frac{W}{2})\):
\(G_{\text{target\_dB}}=T+\frac{X_{\text{dB}}-T}{R}\)
Once \(G_{\text{target\_dB}}\) is parsed, compute the final attenuation delta: \(\Delta G_{\text{dB}} = G_{\text{target\_dB}} - X_{\text{dB}}\). Pass this value down to your existing temporal smoothing filters (\(a_{\text{attack}}, a_{\text{release}}\)).

Integration Across Modules:
Wideband Compressor Loop: Apply the math above using the user-adjusted variable compressorKneeWidthDB for the value of \(W\).
Multiband Compressor Loop: Apply this exact same math curve independently to all three processing bands (Low, Mid, High). Hardcode the value of \(W\) to a fixed value of 6.0 internally for all bands.



4. Implementation Steps for the Agent
Preset Architecture: Extend configuration variables to include the cross-over slopes and the wideband knee parameter.
UI Realignment: Update your view stack layout. Inset the slope segmented pickers between the crossover text labels and the frequency sliders. Place the Knee Width slider immediately below the wideband Compressor's ratio control.
DSP Implementation: Import Accelerate inside the DSP core engine blocks. Route the crossover split bands through the multi-stage vDSP_biquad arrays depending on user selection. Replace hard threshold logic with the polynomial knee equations across both the wideband compressor thread and the 3-band multiband process blocks.

B. System Specification: Audio Metering Suite Color Transitions & UI Cleanup

Objective
Integrate an 8-channel visual ballistics metering engine featuring real-time color transitions directly onto the main screen of the macOS equalizer application (DHDook/equaliser-adv). This specification adds dynamic multi-stage color styling (Green to Amber to Red) depending on compression depth and instructs the agent to purge the non-functional legacy Gain Reduction (GR) meter from the dynamics configuration panel layer.





1. UI Layout & Color States Architecture

Main Screen Layout (The 8-Channel Meter Stack)
Action: Render a vertical stack of eight (8) ultra-narrow LED-style meters positioned directly to the left of the existing dynamics bypass toggles.
Visual Direction: Meters 1 through 7 must rest at \(0\text{ dB}\) (top of the meter frame) and drop downward into negative decibels (\(0\text{ dB}\) to \(-24\text{ dB}\)) to represent gain attenuation. Meter 8 (Clipper) must feature a 1.5-second sticky peak-hold indicator line.
Sizing Constraint: Scale the horizontal width of each meter element tightly so that adding 8 meters side-by-side does not force the main application window to expand or distort existing padding.

Real-Time LED Color Threshold Matrix
To match premium studio hardware hardware, the meter bars must transition color dynamically based on their current active compression depth (\(G_{\text{current\_dB}}\)):
Zone 1: Transparent / Light Target Compression (\(0.0\text{ dB}\) to \(-4.0\text{ dB}\))
Color: Solid Premium Green (Hex: #2ECC71 or system equivalent).
Sonic Meaning: Safe, standard operational level.
Zone 2: Moderate Glue / Transient Management (\(-4.01\text{ dB}\) to \(-12.0\text{ dB}\))
Color: Linear Interpolation from Green to Warm Amber (Hex: #F39C12).
Sonic Meaning: Active heavy processing; the signal is being squeezed aggressively.
Zone 3: Heavy Over-Compression / Limit Squashing (\(-12.01\text{ dB}\) to \(-24.0\text{ dB}\) or lower)
Color: Linear Interpolation from Amber to Warning Red (Hex: #E74C3C).
Sonic Meaning: Extreme attenuation. Potential loss of audio dynamics; warns the user of heavy squashing.

Dynamics Menu Legacy Cleanup
Action: Locate the legacy codebase files rendering the sub-panel layers inside the dynamics configuration popover/sheet menu.
Purge Rule: Completely delete the non-functional, broken GR meter component, its backing canvas draw references, and all associated layout constraints from this view hierarchy to clean up the workspace.



2. State & Meter Tracking Variables
Add these parameters to the interface state controller file or your rendering layer container thread to manage the smoothed positions of the visual bars:

swift
var meterDeesserGR: Float = 0.0
var meterMBLowGR: Float = 0.0
var meterMBMidGR: Float = 0.0
var meterMBHighGR: Float = 0.0
var meterCompressorGR: Float = 0.0
var meterExpanderGR: Float = 0.0
var meterClipperGR: Float = 0.0
var meterLimiterGR: Float = 0.0
var clipperPeakHoldTimer: Double = 0.0
var clipperPeakHoldValue: Float = 0.0
Use code with caution.




3. Core Math, Visual Ballistics, & Color Interpolation Logic
Execute raw data capture inside your background CoreAudio thread blocks and pipe the variables down to the UI Main Thread via a frame callback timer running at \(60\text{ Hz}\).

Part A: Ballistics Smoothing Filter (Attack & Release)
Meter Attack (Instant drop): Meters must drop downward instantly to show audio clamping (\(\text{Instant} / 0\text{ ms}\)).
Meter Decay (Smooth return to } 0\text{ dB}\text{):** Use a release time constant of **\(250\text{ ms}\).
Calculate the visual decay coefficient (\(\alpha _{\text{ui}}\)) based on your UI frame refresh rate (\(f_{\text{ui}} = 60\text{ Hz}\)):
\(\alpha _{\text{ui}}=\exp \left(-\frac{1.0}{f_{\text{ui}}\cdot 0.250}\right)\)
On every UI frame refresh tick (60 times per second), update the visual display value for each meter using this smoothing branch logic:

swift
if rawGR < currentMeterValue {
    currentMeterValue = rawGR // Attack phase: Drop downward instantly to peak attenuation level
} else {
    currentMeterValue = (alpha_ui * currentMeterValue) + ((1.0 - alpha_ui) * 0.0) // Release phase: Glide back up toward 0 dB
}
Use code with caution.


Part B: Linear Color Interpolation Math
Instruct the agent to map the visual fill color of the progress bar using a normalized linear blending scalar (\(t \in [0.0, 1.0]\)) inside the rendering loop.
Let \(V = \text{abs}(currentMeterValue)\). Compute the red, green, and blue components via this conditional structure:
If \(V \leq 4.0\):
Set the color strictly to the Base Green (R: 46, G: 204, B: 113).
If \(V > 4.0\) and \(V \leq 12.0\):
Normalize position within the Amber zone: \(t = \frac{V - 4.0}{8.0}\)
Interpolate between Green (46, 204, 113) and Amber (243, 156, 18):
\(R_{\text{render}}=46+t\cdot (243-46)\)
\(G_{\text{render}}=204+t\cdot (156-204)\)
\(B_{\text{render}}=113+t\cdot (18-113)\)
If \(V > 12.0\):
Normalize position within the Red zone: \(t=\min \left(1.0,\frac{V-12.0}{12.0}\right)\)
Interpolate between Amber (243, 156, 18) and Red (231, 76, 60):
\(R_{\text{render}}=243+t\cdot (231-243)\)
\(G_{\text{render}}=156+t\cdot (76-156)\)
\(B_{\text{render}}=18+t\cdot (60-18)\)

Part C: Clipper Peak-Hold Sticky Logic
For Meter 8 (Clipper), apply this dedicated evaluation block during the UI frame refresh loop to lock the maximum peak indicator line on-screen:

swift
if rawClipperGR < clipperPeakHoldValue {
    clipperPeakHoldValue = rawClipperGR
    clipperPeakHoldTimer = 1.5 // Reset the 1.5-second timer
} else {
    if clipperPeakHoldTimer > 0.0 {
        clipperPeakHoldTimer -= (1.0 / 60.0) // Count down the timer using the frame delta time
    } else {
        clipperPeakHoldValue = (alpha_ui * clipperPeakHoldValue) // Timer expired: Decay the line back to 0 dB
    }
}
Use code with caution.




4. Verification & Unit Testing Automation Routine
Inject this automated validation test block inside your test harness to verify that the UI data pipeline calculates, smooths, and translates colors correctly:

swift
func runMeteringEngineUnitTest() {
    print("--- STARTING EQUALISER-ADV METERING ENGINE SUITE TEST ROUTINE ---")
    
    // Test Scenario 1: Verify Visual Ballistics Decay Timing Logic
    var initialMeterValue: Float = -20.0 // Simulate an instantaneous attenuation spike
    let decayCoefficient = Float(exp(-1.0 / (60.0 * 0.250))) // 250ms decay window at 60Hz
    
    // Simulate one single visual frame update tick
    initialMeterValue = (decayCoefficient * initialMeterValue) + ((1.0 - decayCoefficient) * 0.0)
    
    assert(initialMeterValue > -20.0, "Meter ballistics decay must smoothly move back up towards 0 dB over time.")
    print("✔ Scenario 1 Passed: Visual Ballistics decay routine confirmed operational. Incremental Frame Step: \(initialMeterValue) dB")
    
    // Test Scenario 2: Verify Color Matrix Mapping at Hard Comp Limits (-8 dB should be in Amber zone)
    let testValueAmberZone: Float = -8.0
    let absV = abs(testValueAmberZone)
    let targetT = (absV - 4.0) / 8.0
    let evaluatedRedChannel = 46.0 + targetT * (243.0 - 46.0)
    
    assert(evaluatedRedChannel > 46.0 && evaluatedRedChannel < 243.0, "Color engine interpolation matrix mapped to incorrect color channels.")
    print("✔ Scenario 2 Passed: Dynamic RGB interpolation verified inside the Amber warning zone. Calculated Red Channel: \(evaluatedRedChannel)")
    
    print("--- ALL METERING ENGINE SUITE ENVIRONMENT CHECKS PASSED ---")
}
Use code with caution.




5. Implementation Action Steps for the Agent
Purge Panel Asset: Scan the file structure mapping out the dynamic sliders panel view layer. Locate and scrub out the non-functional legacy GR meter component block alongside its layouts.
Mount Visual Meters: Open your main view controller layout files. Draw and stack the 8 narrow vertical progression bar vectors to the left of your master dynamics toggles.
Deploy Color Pipeline: Apply the conditional RGB logic blocks inside your view-layer drawing loops (such as CoreGraphics or SwiftUI background fills) so that the color updates automatically as the meter levels slide up and down.







C. System Specification: Three-Band Frequency-Dependent Stereo Widener Integration

Objective
Integrate a high-end, three-band frequency-dependent Stereo Widener / Spatial Imager module into the macOS equalizer application (DHDook/equaliser-adv). This specification handles adding a single new master toggle to the main screen, a dedicated configuration block inside the Dynamics menu utilizing three band-specific width sliders, and the underlying Mid/Side crossover DSP logic.



1. UI Layout & Component Sequencing

Main Screen Layout (The Toggle Stack)
Action: Add a single master bypass toggle for the Stereo Widener.
Placement Constraints: Position the Stereo Widener Toggle directly below the Dynamic Range Expander Toggle and directly above the Clipper Toggle.
Layout Shift: Programmatically shift both the Clipper Toggle and Limiter Toggle (along with their associated meters, Meter 7 and Meter 8) downward to accommodate this new row without overlapping elements.
Metering Note: Do not create a new visual meter for this control. The 8-channel meter stack remains exactly as previously built.

Dynamics Menu Structure (The Slider Panel)
Placement & Labeling: Inject a new settings block inside the Dynamics configuration menu. Position this new section directly below the Expander section and directly above the Clipper section. Title this section with the exact text heading: "Stereo Widener".
Controls: Populate this section with three horizontal linear layout sliders. Strict UI Rule: To save window space and preserve the no vertical scrolling constraint, do not add any crossover frequency sliders here. Hardcode the frequency split boundaries internally in the DSP code.
Low Width Slider: Range: 0% (Pure Mono) to 100% (Normal Stereo).
Mid Width Slider: Range: 100% (Original Stereo) to 200% (Maximum expansion).
High Width Slider: Range: 100% (Original Stereo) to 200% (Maximum expansion).

Menu Auto-Sizing Behavior
Adjust the window height parameters or main stack constraints of the Dynamics menu view to expand smoothly to hold these three new tightly packed sliders. Ensure the final rendered view fits perfectly on-screen so that no vertical scrolling is introduced.



2. Initialization & Safe Default Profiles
Initialize the variables within your preset serialization architecture using these optimized, playback-neutral defaults. This ensures that when a user switches the module on, the low-end bass stays focused while the mid-range guitars expand cleanly:
Stereo Widener State: OFF
Low Width Default: 0% (Maps internally to a scalar of 0.0. This forces sub-bass elements to pure mono to maintain maximum speaker punch).
Mid Width Default: 140% (Maps internally to a scalar of 1.4. Explodes heavy guitar tracks outward).
High Width Default: 125% (Maps internally to a scalar of 1.25. Adds premium air and spatial depth).

Global Configuration State (EQ Maps Serialization)

swift
var stereoWidenerEnabled: Bool = false
var widthFactorLow: Float = 0.0   // 0% (Pure Mono)
var widthFactorMid: Float = 1.4   // 140% Width
var widthFactorHigh: Float = 1.25 // 125% Width
Use code with caution.




3. Core DSP Mathematical Framework
Inject this frequency-split matrix routing block inside your CoreAudio sample processing loop. To ensure your dynamics processors react accurately to the altered spatial energy, this engine must execute after the Parametric EQ stages, but immediately before the existing dynamics suite (De-Esser -> Multiband -> Wideband Compressor -> Expander).

Step A: Crossover Band Splitting
Using cascaded Biquad Filters via Apple's hardware-accelerated Accelerate Framework (vDSP_biquad), split the incoming stereo audio stream into three independent stereo frequency bands using these hardcoded musical boundaries:
Low Band Filters: Low-pass filter cutting at 200 Hz.
Mid Band Filters: Band-pass filter isolating 200 Hz to 4000 Hz.
High Band Filters: High-pass filter cutting at 4000 Hz.

Step B: Mid/Side Encoding & Spatial Modification
For every sample frame window n across each independent frequency band, encode the audio from Left/Right (\(x_{\text{L}}, x_{\text{R}}\)) into Mid/Side (\(M, S\)) channels. Apply the user-defined width factors (\(W_{\text{Low}}, W_{\text{Mid}}, W_{\text{High}}\)), and implement inverse center attenuation to balance total energy:
Low Band Processing:
\(M_{\text{low}}=0.5\cdot (x_{\text{L\_low}}+x_{\text{R\_low}}),\quad S_{\text{low}}=0.5\cdot (x_{\text{L\_low}}-x_{\text{R\_low}})\)
\(M_{\text{low\_scaled}}=M_{\text{low}}\cdot (2.0-W_{\text{Low}}),\quad S_{\text{low\_scaled}}=S_{\text{low}}\cdot W_{\text{Low}}\)
Mid Band Processing:
\(M_{\text{mid}}=0.5\cdot (x_{\text{L\_mid}}+x_{\text{R\_mid}}),\quad S_{\text{mid}}=0.5\cdot (x_{\text{L\_mid}}-x_{\text{R\_mid}})\)
\(M_{\text{mid\_scaled}}=M_{\text{mid}}\cdot (2.0-W_{\text{Mid}}),\quad S_{\text{mid\_scaled}}=S_{\text{mid}}\cdot W_{\text{Mid}}\)
High Band Processing:
\(M_{\text{high}}=0.5\cdot (x_{\text{L\_high}}+x_{\text{R\_high}}),\quad S_{\text{high}}=0.5\cdot (x_{\text{L\_high}}-x_{\text{R\_high}})\)
\(M_{\text{high\_scaled}}=M_{\text{high}}\cdot (2.0-W_{\text{High}}),\quad S_{\text{high\_scaled}}=S_{\text{high}}\cdot W_{\text{High}}\)

Step C: Mid/Side Decoding & Summation
Decode each band back into Left and Right configurations and sum the three separate frequency bands back together into a single unified stereo buffer before passing it to the De-Esser stage:
\(x_{\text{L\_band}}=M_{\text{scaled}}+S_{\text{scaled}}\)
\(x_{\text{R\_band}}=M_{\text{scaled}}-S_{\text{scaled}}\)
\(x_{\text{L\_final\_out}}[n]=x_{\text{L\_low\_out}}+x_{\text{L\_mid\_out}}+x_{\text{L\_high\_out}}\)
\(x_{\text{R\_final\_out}}[n]=x_{\text{R\_low\_out}}+x_{\text{R\_mid\_out}}+x_{\text{R\_high\_out}}\)



4. Implementation Steps for the Agent
Preset Serialization: Integrate stereoWidenerEnabled, widthFactorLow, widthFactorMid, and widthFactorHigh variables cleanly into your preset map loading/saving files.
UI Assembly: Modify the main window layout view. Drop the new master toggle into the space between the Expander and Clipper controls, shifting lower elements down. In the sliders panel, create the "Stereo Widener" section right below the Expander section, adjusting the parent window constraint parameters to prevent any scrollbars from appearing.
vDSP Pipeline Injection: Locate your CoreAudio rendering loop thread. Insert the three-band filter crossover and Mid/Side matrix math right after your core equalizer array output. The combined summed output of this spatial engine must become the direct input vector for your De-Esser processing stage.






D. ### Sub-System Specification: Real-Time LUFS Loudness Matching Module

#### 1. UI Layout & Component Sequencing
* Action: Add a master bypass toggle to the main screen, and add a single configuration slider to the Dynamics menu.
* Main Screen Toggle Placement: Position the "Loudness Match Toggle" directly below your "Stereo Widener Toggle" and directly above the "Clipper Toggle".
* Layout Shift: Programmatically shift down the Clipper and Limiter rows (along with their meters) to fit this new row without causing visual overlap.
* Dynamics Slider Section: Create a new parameter section titled "Loudness Match" directly below the Stereo Widener section. Populate it with one linear slider:
    - Slider Label: "Target Level"
    - Range: -24.0 LUFS to -10.0 LUFS
    - Safe Default: -16.0 LUFS
* Layout Window Bounds: Enforce auto-scaling height properties to ensure the parent Dynamics menu window fits this parameter row completely without introducing vertical scrollbars.

#### 2. State & Preset Serialization
Map these parameters inside your core serialization modules to ensure values save alongside individual EQ presets:
```swift
var loudnessMatchingEnabled: Bool = false
var targetLoudnessLUFS: Float = -16.0
var liveSmoothedGainModifier: Float = 1.0
```

#### 3. Core DSP Mathematical Framework
This module calculates metrics by monitoring the pipeline, but applies its target leveling multiplier as the absolute first node of your Dynamics Chain (Executing after the Parametric EQ and Stereo Widener, but before the De-Esser, Compressors, and Expanders).

* Step A (K-Weighting): Clone the stereo buffer and apply a 2nd-order high-shelving filter (+4 dB at 1 kHz) followed by a 2nd-order high-pass filter (-3 dB at 100 Hz).
* Step B (Short-Term Block Power): Calculate the mean square power over the block length. Buffer these values across a sliding FIFO ring buffer tracking a precise 3.0-second timeline.
* Step C (Calculate Error Correction): Read the average decibel power level of the active blocks inside the 3.0-second queue (ignoring blocks reading below -70.0 dB). Let this computed running metric be 'ShortTermLUFS'.
* Step D (Gain Smoothing Engine): Calculate the instantaneous correction target:
    GainDeltaDB = targetLoudnessLUFS - ShortTermLUFS
  
  To prevent noticeable "pumping" artifacts or sudden volume changes during transitions, smooth this GainDeltaDB value using a highly relaxed, slow visual-ballistic filter coefficient tracking a 2.0-second integration time window.
  Convert the resulting smoothed decibel correction into a linear scalar multiplier:
    liveSmoothedGainModifier = pow(10.0, smoothedCorrectionDB / 20.0)

* Step E (Application): Multiply the incoming audio frame values evenly by this scalar variable before handing the data array to the De-Esser stage:
    x_left[n] = x_left[n] * liveSmoothedGainModifier
    x_right[n] = x_right[n] * liveSmoothedGainModifier


E. System Specification: UI Grid Restructuring & Signal Chain Synchronization


Objective
Restructure the main view screen layout, tooltip strings, and the Dynamics configuration menu of the macOS equalizer application (DHDook/equaliser-adv) into an efficient, functional two-column grid format. This architecture economizes vertical screen real estate, completely eliminates vertical scrolling, maintains clear side-by-side meter/toggle pairs, and synchronizes the visual controls with the exact processing order of the CoreAudio pipeline.



1. Main View UI: Functional Two-Column Grid Layout

Structural Optimization
Action: Transform the main view dashboard into a clean two-column grid.
Column 1 (Left Side - Metered Dynamics Suite): Dedicated entirely to threshold-dependent dynamics processors. Every row in this column must physically mount a visual Gain Reduction (GR) meter directly to the left of its associated Master Bypass Toggle.
Column 2 (Right Side - Spatial & Utility Suite): Dedicated to spatial imaging and volume calibration. This column contains only standalone Master Bypass Toggles with no attached visual meters.

Precise Row-by-Row Layout Blueprint

text
========================================================================
[COLUMN 1: METERED DYNAMICS (LEFT)]      [COLUMN 2: SPATIAL & UTILITY (RIGHT)]
========================================================================
Each row below is a side-by-side pair:   Standalone toggles (No meters):
--------------------------------------   -------------------------------------
Row 1: [De-Esser Meter]  -> [Toggle]      Row 1: [Stereo Widener Toggle]
Row 2: [MB Low Meter]  ┐                 Row 2: [Loudness Match Toggle]
Row 3: [MB Mid Meter]  ├─> [Toggle]       
Row 4: [MB High Meter] ┘                 
Row 5: [Compressor Meter] -> [Toggle]     
Row 6: [Expander Meter]   -> [Toggle]     
Row 7: [Clipper Meter+Hold]-> [Toggle]     
Row 8: [Limiter Meter]    -> [Toggle]     
Use code with caution.




2. Tooltip Definition Updates
Action: Update the unified text tooltip box situated above the layout grid to clearly display definitions matching the synchronized signal chain sequence.
Exact Text String to Inject:
"Stereo Widener: Adjusts multi-band spatial fields to expand guitars and ambient sounds while keeping low bass centered. Loudness Match: Automatically smooths out long-term volume offsets between disparate source tracks to a stable target LUFS baseline. De-Esser: Tames harsh high-frequency sibilance ('S' and 'T' vocal sounds). Multiband Compressor: Separately controls the dynamics of independent low, mid, and high frequency bands. Compressor: Balances the wideband dynamic range to glue the stereo mix together. Expander: Gently attenuates sub-threshold elements to lower background noise or expand flat mixes. Clipper: Shaves microsecond transient peaks to maximize clean headroom. Limiter: Acts as an absolute brickwall ceiling to stop digital clipping distortion."



3. Dynamics Configuration Panel Restructuring

Grid Transformation
Action: Convert the Dynamics configuration menu from a long vertical list into a clean, two-column horizontal module grid.
Layout Sequencing: Distribute the parameter sections evenly to match the logical layout of the main dashboard. Group the controls tightly so that the parent window frame auto-sizes width-wise, enforcing the strict rule of zero vertical scrolling.
========================================================================
[DYNAMICS CONFIGURATION PANEL - TWO COLUMN GRID FORMAT]
========================================================================
COLUMN 1 (Left Side Modules):           COLUMN 2 (Right Side Modules):
------------------------------------    --------------------------------
1. "Stereo Widener" Section             4. "Compressor" Section
   - Low Width Slider                      - Threshold Slider
   - Mid Width Slider                      - Ratio Slider
   - High Width Slider                     - Knee Width Slider
                                           - Attack Time Slider
2. "Loudness Match" Section                - Release Time Slider
   - Target Level Slider                   - Makeup Gain Slider

3. "De-Esser" Section                   5. "Expander" Section
   - Frequency Slider                      - Threshold Slider
   - Threshold Slider                      - Ratio Slider
                                           - Range Slider
3b. "Multiband Compressor" Section 
   - Low/Mid Freq Slider                6. "Clipper" Section
   - Low/Mid Slope [Gentle/Steep]          - Ceiling / Threshold Sliders
   - Mid/High Freq Slider
   - Mid/High Slope [Gentle/Steep]      7. "Limiter" Section
   - Low Band Threshold Slider             - Release / Ceiling Sliders
   - Mid Band Threshold Slider
   - High Band Threshold Slider
Use code with caution.

Legacy Cleanup Reminder: Prior to rendering this layout, ensure any legacy references to the non-functional GR meter inside the dynamics panel files are completely removed from the view hierarchy.



4. Direct CoreAudio DSP Execution Pipeline Synchronization
Ensure that your background processing threads execute the incoming stereo buffer blocks in this exact chronological order. The output vectors of each module must act as the clean input vectors for the subsequent stage:
Stage 1: Parametric EQ Engine (Core multi-band tonal adjustments)
Stage 2: Three-Band Frequency-Dependent Stereo Widener (Spatial processing)
Stage 3: Real-Time LUFS Loudness Matching (Establishes uniform volume baseline)
Stage 4: De-Esser (High-frequency sibilance clean-up)
Stage 5: Multiband Compressor (Independent band energy control)
Stage 6: Wideband Compressor (Main dynamic glue and soft-knee processing)
Stage 7: Dynamic Range Expander (Macro dynamic restoration)
Stage 8: Clipper (Instantaneous sample peak clipping)
Stage 9: Limiter (Final protective ceiling output safety brickwall)



5. Implementation Action Steps for the Agent
Main Interface Geometry: Modify your primary main screen UI files. Rearrange the layout containers into the two functional columns specified in Section 1. Ensure the 8 meters snap directly to the left boundary of their corresponding toggles in Column 1, and place the standalone switches inside Column 2.
Dynamics Interface Constraints: Open your sub-panel/sheet menu files. Separate the parameter blocks into Left and Right column arrays as specified in Section 3. Ensure your window bounds automatically stretch wide rather than long, suppressing vertical scrollbar creation.
DSP Callback Flow Control: Audit your core CoreAudio sample render loops. Ensure the array pointers process through the 9 structural stages sequentially according to the exact list defined in Section 4.


F. Specification Set 1: CoreAudio Bit-Perfect Stream Monitoring Module (Up to 384 kHz)


Objective
Integrate a hardware-level Bit-Perfect Stream Monitoring and Automatic Clock Switching engine into the macOS equalizer application (DHDook/equaliser-adv). This specification instructs the agent to create an active listener that monitors incoming media sample rates (e.g., Apple Music, lossless media files, browser streams) up to 384 kHz and dynamically re-locks both the application’s internal vDSP coefficient calculators and the external DAC hardware to match the source file natively, eliminating destructive system resampling.



1. UI Layout & Component Sequencing

Main View Stack Addition
Action: Add a high-contrast text status label and a master configuration checkbox.
Placement Constraints: Position this text block inside Column 2 as Row 6 (Right Side), directly beneath the Time and Stereo Balance control group.
Label Text Strings:
Checkbox: "Bit-Perfect Mode"
Status Indicator: "Stream Status: [Active Rate]" (e.g., Stream Status: 384.0 kHz (Bit-Perfect))
Layout Window Bounds: Enforce strict auto-expanding vertical constraints to hold this row element cleanly, ensuring the parent main screen dashboard adheres to the strict zero vertical scrolling rule.

Tooltip Update
Action: Update the unified text tooltip box situated above the main screen grid layout to append the functional definition for this tracking system.
Text String to Append:
"Stream Status: Monitors the source media's native sampling rate in real time up to 384 kHz to dynamically change CoreAudio hardware clock parameters, ensuring unresampled, bit-perfect audio playback."



2. State & Preset Serialization
Map these new system parameters into your global state container structures to ensure they serialize cleanly alongside individual EQ maps:

swift
var hardwareBitPerfectMatchingEnabled: Bool = true
var activeSystemSampleRate: Double = 44100.0 // Stored current operational rate (Supports values up to 384000.0)
Use code with caution.




3. Core CoreAudio HW Optimization & Re-Clocking Logic
Instruct the engine to configure a system-level event listener loop capable of intercepting format modifications safely without causing thread crashes.

Step A: Stream Format Property Listener
Inside your CoreAudio initialization block or hardware device abstraction class, hook a global listener property block to monitor changes in the active system audio stream configuration via the CoreAudio hardware API:

swift
// REFACTOR ACTION: Establish Native Format Listener Hook
var propertyAddress = AudioObjectPropertyAddress(
    mSelector: kAudioDevicePropertyStreamFormat,
    mScope: kAudioObjectPropertyScopeInput,
    mElement: kAudioObjectPropertyElementMain
)

// Register a listener callback to catch native player adjustments instantly
AudioObjectAddPropertyListener(targetDeviceID, &propertyAddress, formatListenerCallback, nil)
Use code with caution.


Step B: Dynamic Hardware Clock Re-locking (Extended Target Matrix)
Inside the formatListenerCallback logic loop, execute the following state branch whenever a change in the source media's sample rate is detected:
Check the atomic boolean state of hardwareBitPerfectMatchingEnabled. If false, bypass the routine and continue standard rendering.
If true, extract the newly requested sample rate floating-point value. Ensure your verification checks accept the full high-fidelity processing matrix: 44100.0, 48000.0, 88200.0, 96000.0, 176400.0, 192000.0, 352800.0, or 384000.0 Hz.
Safely pause or flush the active CoreAudio rendering ring buffers to prevent audio clicking or pop artifacts.
Invoke AudioObjectSetPropertyData to force your external DAC hardware device clock to lock onto the new native sample rate.
Set your internal activeSystemSampleRate atomic property to match. This forces all downstream vDSPcoefficient math setups (e.g., your crossover slopes, tilt filters, and dynamic EQs) to immediately re-calculate their digital bounds using the updated sample rate (\(f_{s}\)), preserving exact acoustic curves without audio thread drops up to the 384 kHz boundary.



4. Implementation Steps for the Agent
Refactor UI Constraints: Update your 2-column main screen layout file. Inject the checkbox and status text block into Column 2 below the balance matrix controls. Update the main text tooltips with the appended string block.
Deploy CoreAudio Format Listener: Implement the hardware-level property listeners in your CoreAudio initialization thread layer to intercept kAudioDevicePropertyStreamFormat data updates.
Bridge State Channels via Atomics: Ensure the intercepted sample rate updates thread cleanly into the variable parameter modules using lock-free .store() routines, allowing your active vDSP_biquadfunctions to re-scale seamlessly.